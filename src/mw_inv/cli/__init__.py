"""Command-line entrypoints for mw_inv.

These wrap the repo's `scripts/` so users can either:
- run `python scripts/...` in a checkout, or
- install the package and use console scripts (`mw-inv-*`).
"""

