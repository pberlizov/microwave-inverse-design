# Thin-slice runs on numpy/scipy/optuna only -- no MEEP, no conda.
numpy>=1.26
scipy>=1.11
optuna>=3.6
# Optional: figures. matplotlib < 3.11 has a savefig RecursionError on CPython 3.14;
# 3.11 fixes it. Scripts degrade gracefully (write .npz/.json) if matplotlib is missing.
matplotlib>=3.11
pytest>=8.0
